pipeline{
		agent any
		stages{
				stage('Submit Stack'){
					steps{										
					sh "aws cloudformation create-stack --stack-name tomcatappdemo --template-body file://ecinstance.yml --tags Key=Name,Value=testserver --region 'us-west-2'"
					}
				}
		}
}
