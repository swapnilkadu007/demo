pipeline{
		agent any
		stages{
				stage('Submit Stack'){
					steps{					
					sh "pwd"					
					sh "aws cloudformation create-stack --stack-name ecinstancetest --template-body file://cf_script.json --tags Key=Name,Value=testserver Key=costid,Value=123 --region 'us-west-2'"
					}
				}
		}
}
