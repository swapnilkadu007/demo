pipeline{
		agent any
		stages{
				stage('Submit Stack'){
					steps{										
					sh "sudo aws cloudformation create-stack --stack-name tomcatapptdemo --template-body file://ecinstance.yml --tags Key=Name,Value=testserver --region 'us-west-2'"
					}
				}
		}
}
